package com.cex.core.risk;

/**
 * 风控规则对订单给出的处理决定。
 * 核心能力：指示订单直接通过或进入人工审核；线程安全：枚举实例不可变且天然线程安全；使用限制：规则实现仅可返回此两种确定结论。
 */
public enum RiskDecision {
    /** 订单未触发当前风控规则，可继续处理。 */
    PASS,
    /** 订单触发风险阈值，必须等待人工审批。 */
    HOLD
}
